@mainpage behavior_trees

# behavior_trees

**behavior_trees** is a header-only C++26 library for composable behavior trees, used by the l5 game engine.

## Source Layout

- `include/` — Public headers

## Building

behavior_trees is a header-only library included automatically when building l5:

```sh
task build-linux
```
